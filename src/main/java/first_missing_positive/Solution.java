package first_missing_positive;

class Solution {
    public int firstMissingPositive(int[] nums) {
        // find 1
        // 1 is absent - return 1
        // find next gap after 1
        // return min in a gap
        // if no gap is present - return max value + 1
        int max = 0;
        for (int i = 0; i < nums.length; i++) {

        }
        return 0;
    }
}
